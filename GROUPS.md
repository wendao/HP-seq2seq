# Grouped Split Analysis

## 背景

`--split grouped` 按输出序列分组划分训练/验证集，保证同一输出序列不会同时出现在训练集和验证集中。相比随机划分，这更诚实地衡量模型的泛化能力——模型必须预测从未见过的输出序列。

数据集：24900 样本，5310 种唯一输出，5-fold 分组后各 fold 恰好 4980 样本。

## 结果对比

全部数据来自 2026-08-20 的重跑，seed 42，MPS。RNN / LSTM / CNN 为 100 epochs、
`--num_layers 1 --hidden_size 128 --embed_dim 64`（CNN 为 3 层），来源
`logs/*_{random,grouped}_?.log`。

| Model | Random Split | Grouped Split | 衰减 |
|-------|-------------|--------------|------|
| LSTM + CA | 56.43% | 19.27% | −65.8% |
| LSTM | 44.16% | 10.53% | −76.2% |
| RNN + CA | 37.83% | 10.98% | −71.0% |
| RNN | 23.44% | 2.64% | −88.8% |
| CNN + CA | 12.78% | 3.27% | −74.4% |
| CNN (3层) | 12.59% | 2.94% | −76.6% |
| — | | | |
| **Transformer（`train.py`）** | — | **59.95%** | — |

`logs/baseline_boxplot.html` 是前六行的箱线图（含逐折分布）。

### 关于 Transformer 那一行

**它与上面六行不是同一口径**：4 层、EMBED 96、HIDDEN 512、1,243,654 参数、
200 epochs、AdamW + warmup/cosine，来源 `logs_trf/trf_grp_*.log`（5 折均值 0.5995，
标准差 0.0157）。基线是 1 层、约 10–40 万参数、100 epochs、朴素 Adam。所以这一行
回答的是"这个任务能做到多好"，不是"同等预算下 Transformer 强多少"。

它也**没有随机划分的对照**，因此"衰减"一列为空。

`train_baseline.py --model transformer` 本可提供同口径的 1 层对照，但在基线设置下
（朴素 Adam、无调度、100 轮）它跑不到收敛——loss 到最后一轮仍在下降，且它是唯一
train hit rate 低于 val 的模型。那次结果已从矩阵和日志中移除，因为它度量的是训练预算
而不是架构。

**所以目前不存在一次受控的架构对比。** 唯一一次同预算的尝试因欠训而作废，而 0.5995
来自一个参数量大三倍、轮数翻倍、调过调度器的模型。"这个任务上最好的结果由 transformer
取得"成立；"transformer 架构在同等预算下优于 LSTM"没有被证明。

要补上这一格，需要把 1 层 Transformer 训到收敛（加 warmup + cosine，轮数提到 400），
或者反过来给 LSTM+CA 同样的 200 轮和调度器。前者更省时间。

> 本文件的早期版本有一行 "Transformer (1层)：随机 44.37% / 分组 33.61%"，
> 仓库里没有任何日志对应它，已移除。早期版本的 LSTM 42.35% 与 RNN 21.43% 则混合了
> 两种不同架构的日志（详见 README 的"基线的状态"一节），也已被上表的重跑数据取代。

## 分析

### 为什么 RNN/LSTM/CNN 在 grouped split 下表现很差？

**随机划分的虚假高分。** 随机划分时，相同的输出序列可能同时出现在训练集和验证集。RNN/LSTM 善于学习局部的输入→输出映射关系，当验证集中的输入与训练集中产生相同输出的输入相似时，模型可以靠模式匹配"作弊"，而非真正理解 HP→结构的折叠规则。

**局部感受野限制泛化。** RNN/LSTM 逐 token 处理序列，CNN 的卷积核也只有局部视野。这些架构倾向于学习局部的序列对应关系（如 "HPH...→RFL..."），而不是全局的抽象折叠规则。当验证集的输出完全未见过时，局部模式不足以推断正确的全局结构。

### 为什么 Transformer 表现更好？

**全局自注意力。** Transformer 的 self-attention 允许每个输出位置在生成时看到全部输入序列。这种全局感受野使其能够学习更抽象的输入→输出关系，而非仅仅记忆局部模式。

**Cross-attention 已内建。** Transformer decoder 的 cross-attention 可以灵活关注编码器的任意位置，这种全局条件生成能力天然适合从输入序列推断未见过的输出结构。

**归纳偏置差异。** RNN 的归纳偏置是"近大远小"（局部优先），适合序列建模但不利于学习全局折叠规则。Transformer 没有内置的距离偏置，所有位置平等对待，更容易捕捉 HP 序列中远距离残基之间的相互作用。

### Cross-attention 的作用与它的边界

重跑第一次填上了 +CA 的分组划分格子（此前 `train_baseline.py` 没有 `--split`，跑不
出来），于是可以分别看两种划分下的增益：

| | 随机划分 | 增益 | 分组划分 | 增益 |
|---|---|---|---|---|
| LSTM | 0.4416 → 0.5643 | +28% | 0.1053 → 0.1927 | **+83%** |
| RNN | 0.2344 → 0.3783 | +61% | 0.0264 → 0.1098 | **+317%** |
| CNN | 0.1259 → 0.1278 | +1.5%¹ | 0.0294 → 0.0327 | +11%¹ |

¹ CNN 那两格不显著：Welch t 分别为 0.5 和 1.0，五折区间互相重叠，与噪声无法区分。
下面的讨论只针对 LSTM 和 RNN——它们的十个折点完全分离，t 值 13–45。

**CA 的相对增益在分组划分下反而更大。** 这值得强调，因为它反直觉：分组划分要求模型对
没见过的输出结构做泛化，而正是在这个更难的设定下，注意力的作用更突出。合理的解释是
随机划分给了模型一条捷径——靠记忆已见输出就能拿分，注意力提供的全局视野派不上大用；
把捷径堵死之后，"能看到整条输入"才真正开始起作用。

所以 CA 带来的是真实的泛化能力，不是记忆的放大器。

### CA 收益为什么在 CNN 上几乎消失

原因在代码里——`Seq2SeqCNN.forward` 把 `encoder_embed`（没过任何卷积层的原始输入
embedding）当作注意力的 memory，而 RNN 和 LSTM 传的是 `encoder_output`。它的编码器还
把 20 个位置 max-pool 成 1 个。两处瓶颈串联，注意力却加在了错误的一端。

### 一个未被检验的上限

三个基线解码器都用 `self.embedding(x)` 做 Query，也就是**上一个输出 token 的
embedding**，而不是解码器隐状态。输出词表只有 6 个 token，所以查询向量最多只有 6 种
——同一个 token 出现在第 3 位和第 15 位，注意力权重完全相同，它不知道自己在序列的
哪个位置。`train.py` 的 Transformer 则是先加正弦位置编码再进 self-attention，查询是
位置感知的。

这**可能**限制了基线 CA 的上限，但目前没有证据支持这个猜测——上表显示位置盲的注意力
在分组划分下工作得相当好。要检验它，需要把 Query 换成解码器隐状态（或给 embedding 加
位置编码）再跑一次同口径对比。改动很小，尚未做。

### 关键结论

1. **随机划分高估了泛化能力。** LSTM 从 44.16% 掉到 10.53%，RNN 从 23.44% 掉到
   2.64%，衰减 76% 与 89%。大部分"能力"来自记忆已有的输出模式。

2. **衰减幅度本身是个诊断量。** 分组划分与随机划分的差距**就是**记忆的部分。所以
   "衰减小"未必是好事——一个还没学会任何东西的欠训模型也不会掉多少，因为它本来就
   没有可供记忆的内容。要连同绝对分数一起看。

3. **Transformer 真正学到了一些泛化规则。** 59.95% 的分组 hit rate 不仅远超所有基线
   的分组成绩，还高于最强基线 LSTM+CA 在**随机**划分下的 56.43%——也就是说它在"没
   见过的结构"上，比最强基线在"见过答案"的条件下表现更好。不过要记得它的预算也大得多
   （见上方"关于 Transformer 那一行"）。

4. **Grouped split 是更诚实的评估方式。** 对于目标是学习物理/生物学映射的任务，应当
   确保训练和验证的输出不重叠，否则无法区分"记忆"和"理解"。
